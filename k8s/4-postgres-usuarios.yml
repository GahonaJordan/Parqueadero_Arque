apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-usuarios
  namespace: parqueaderos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-usuarios
  template:
    metadata:
      labels:
        app: postgres-usuarios
    spec:
      containers:
      - name: postgres-usuarios
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: "usuarios_db"
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "123456"
        volumeMounts:
        - name: postgres-usuarios-storage
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: postgres-usuarios-storage
        persistentVolumeClaim:
          claimName: postgres-usuarios-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-usuarios-pvc
  namespace: parqueaderos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-usuarios
  namespace: parqueaderos
spec:
  selector:
    app: postgres-usuarios
  ports:
  - port: 5432
    targetPort: 5432
